EBS Lite — Codex Prompt Runbook (exact execution order)
Date: 2026-03-02

Goal
- Make `flutter_app/` and `go_backend_rmt/` release-ready.
- Keep Flutter ↔ OpenAPI ↔ Backend API contracts 1:1 matched.
- Ensure the repo passes all DoD checks:
  - Go: `go test ./...`, `go vet ./...`, `gofmt -l .`
  - Flutter: `flutter analyze`, `flutter test`, `dart format --set-exit-if-changed .`
  - Parity: `python tools/api_parity_check.py --out tools/api_parity_report.md` has 0 missing + 0 method mismatches.

========================================================================
PROMPT 00 — Baseline snapshot (run first, no code changes unless required)
========================================================================
Read:
- `RELEASE_READINESS_PLAN.md`
- `flutter_app/ERP System Requirements Document.txt`
- `go_backend_rmt/Docs & Schema/ERP System Requirements Document.txt`
- `ebs_lite_win/Requirements.txt`

Then run and report results (include exact command output summaries):
1) From repo root:
   - `python -m pip install -r tools/requirements.txt`
   - `python tools/api_parity_check.py --out tools/api_parity_report.md`
2) From `go_backend_rmt/`:
   - `go test ./...`
   - `go vet ./...`
   - `gofmt -l .`
3) From `flutter_app/`:
   - `flutter pub get`
   - `flutter analyze`
   - `flutter test`
   - `dart format --output=none --set-exit-if-changed .`

Acceptance:
- A short checklist of all failing items (grouped by Go / Flutter / Parity / Docs).
- No “hand-wavy” items; each item cites a command/file.

========================================================================
PROMPT 00A — Market scan: feature matrix + common gaps (inform the MVP)
========================================================================
Search the market for comparable SMB POS/ERP products (examples: Lightspeed Retail POS, Odoo POS, ERPNext POS, Shopify POS, Square POS; plus SMB accounting like Zoho Books/QuickBooks for expectations).

Do:
- Produce a concise “feature matrix” grouped by modules:
  - POS selling (returns, split payments, offline, receipts)
  - Cash drawer / sessions (float, cash drops/payouts, X/Z reports, training mode)
  - Inventory (PO/GRN, counts, transfers, reorder, valuation)
  - Customers/loyalty/promotions
  - Admin/RBAC/audit
  - Reports/exports
  - Integrations (printers/barcode scanners, email/WhatsApp share, backups)
- Identify the top 10 “common gaps” those products have (offline sync duplicates, stuck sessions, weak audit trail, etc.).
- Capture explicit offline limitations seen in real POS products (what still works offline vs what must be blocked/queued).
- Update:
  - `MARKET_FEATURE_MATRIX_2026_03_02.md` (keep it short + actionable; must vs defer)
  - `RELEASE_READINESS_IMPROVEMENTS_2026_03_02.md` (add/adjust must-haves + risks)
  - `CODEX_PROMPTS_RELEASE_READY_EXECUTION_ORDER.txt` (insert prompts to address the gaps)

Acceptance:
- The matrix is actionable (it lists what we must implement vs defer).
- The “common gaps” list maps to concrete mitigations we can build.

========================================================================
PROMPT 01 — Formatter-only cleanup (separate, mechanical change)
========================================================================
Make the repo pass formatting gates without behavior changes.

Do:
1) In `go_backend_rmt/`:
   - Run `gofmt -w` only on files that `gofmt -l .` reports.
2) In `flutter_app/`:
   - Run `dart format .` (in-place) and ensure the formatting check passes.

Acceptance:
- Go: `gofmt -l .` prints nothing.
- Flutter: `dart format --set-exit-if-changed .` exits 0.
- Go + Flutter tests still pass.

========================================================================
PROMPT 02 — Secrets hygiene + env discipline (release blocker)
========================================================================
Fix committed secrets and make env configuration production-safe.

Do:
- Remove committed `go_backend_rmt/.env` from version control (replace with `go_backend_rmt/.env.example`).
- Ensure `.env` is ignored (root `.gitignore` and/or `go_backend_rmt/.gitignore`).
- `go_backend_rmt/.env.example` must include:
  - `DATABASE_URL`, `JWT_SECRET`, `ENVIRONMENT`, `REDIS_URL`, `MAX_UPLOAD_SIZE`, `UPLOAD_PATH`, `FRONTEND_BASE_URL`, SMTP fields.
- Update `go_backend_rmt/README.md` with exact “copy + edit” instructions.

Acceptance:
- Repo no longer contains real secrets or default production secrets.
- Dev quickstart still works using `.env.example` → `.env` locally.

========================================================================
PROMPT 03 — Fix Flutter↔OpenAPI parity false-positive for Reports
========================================================================
Current issue: Flutter builds report URLs dynamically (`/reports/$endpoint`) so parity tooling flags a missing `/reports/{}` path even though specific report endpoints exist.

Do (preferred approach):
- Replace dynamic concatenation with explicit endpoint constants that start with `/reports/...`.
  - Keep the UI-driven `ReportConfig` list, but store the full path (e.g. `/reports/sales-summary`) instead of only `sales-summary`.
- Update `flutter_app/lib/features/reports/data/reports_repository.dart` to accept full paths and stop generating `/reports/$endpoint`.
- Re-run `python tools/api_parity_check.py --out tools/api_parity_report.md` and ensure no missing paths/method mismatches.

Acceptance:
- Parity report: “Flutter paths missing from OpenAPI” is empty AND “Method mismatches” is empty.
- Reports still load and export (PDF/Excel) in Flutter (at least via smoke/widget tests or a small repository test).

========================================================================
PROMPT 04 — Add Backend↔OpenAPI contract drift check (missing today)
========================================================================
Add a deterministic way to compare implemented Gin routes to `go_backend_rmt/openapi.yaml`.

Do:
- Add a small route-dump command (no DB connection required) that prints `METHOD PATH` for all Gin routes from `go_backend_rmt/internal/routes/routes.go`.
- Add a new script under `tools/` that:
  - reads the dumped routes
  - reads `go_backend_rmt/openapi.yaml`
  - normalizes Gin params (`:id`) to OpenAPI params (`{id}`)
  - reports:
    - implemented routes missing from OpenAPI
    - OpenAPI routes missing from implementation
- Add docs in `tools/` on how to run it.

Acceptance:
- The check can run on a clean machine and produces stable output.
- The check supports `/health` being served at `/` while the API base is `/api/v1`.

========================================================================
PROMPT 05 — Make `go_backend_rmt/openapi.yaml` match implemented routes
========================================================================
Use the backend route dump (from Prompt 04) as the source of truth.

Do:
- Update `go_backend_rmt/openapi.yaml` so the backend↔OpenAPI drift report is empty.
- Do not change runtime behavior (routes/handlers) while fixing docs-only drift.

Acceptance:
- Backend↔OpenAPI drift report is empty (0 missing both directions).
- Flutter↔OpenAPI parity report still has 0 missing paths and 0 mismatches.

========================================================================
PROMPT 06 — Harden file uploads (size/type/name) + update OpenAPI
========================================================================
Current risk: uploads do not enforce `MAX_UPLOAD_SIZE`, do not validate types, and filenames are guessable.

Do:
- Add a global upload size limiter middleware in Go (applies to multipart endpoints).
- Enforce an allowlist for upload types:
  - company logos: png/jpg/webp (reasonable)
  - purchase invoices/receipts: pdf + common image types (reasonable)
- Store uploads with random names (UUID), never trust client filename.
- Ensure uploads are stored only under configured `UPLOAD_PATH`.
- Update `go_backend_rmt/openapi.yaml` for multipart endpoints:
  - `/companies/{id}/logo`
  - `/purchases/{id}/invoice`
  - `/purchase-returns/{id}/receipt`

Acceptance:
- Requests exceeding size limit fail fast with a consistent JSON error.
- Invalid content types/extensions are rejected.
- Go tests pass; add at least 1 unit/integration test for the size/type enforcement.
//////////////////////////////////////////////////////////////////////////////////////////
========================================================================
PROMPT 07 — Migrations system (stop relying on “schema validation only”)
========================================================================
Current state: SQL migrations exist under `go_backend_rmt/Docs & Schema/migrations/` but are not applied automatically.

Do:
- Introduce an actual migrations mechanism (choose one and document it):
  - `golang-migrate/migrate` OR `pressly/goose` (either is fine).
- Ensure migrations include:
  - `2026_01_21_fix_missing_schema_columns.sql`
  - `2026_03_02_add_idempotency_purchases_collections.sql`
- Update Docker/dev bootstrap so a clean `docker-compose up` runs migrations automatically.

Acceptance:
- A brand-new DB can be bootstrapped automatically and the API starts successfully.
- `database.ValidateSchema(...)` still passes (or is replaced by stronger checks).

========================================================================
PROMPT 07A — Operational readiness: backup/restore + support bundle
========================================================================
Market expectation: stores need a simple backup/restore path and support needs diagnostics.

Do:
- Backend:
  - Document a minimal backup plan (pg_dump commands, frequency, retention).
  - Add a `/ready` endpoint (or equivalent) that validates DB + Redis connectivity for deployments.
  - Add a “support bundle” endpoint or CLI command (non-production by default) that exports:
    - app version/build info
    - recent server logs summary (safe/redacted)
    - key configuration flags (non-secret)
- Flutter:
  - Add a “Generate support bundle” action in Settings:
    - includes app version + platform + failed outbox items + last sync errors

Acceptance:
- A non-technical operator can follow docs to back up and restore.
- Support bundle does not leak secrets (tokens/passwords).

========================================================================
PROMPT 08 — Idempotency correctness for offline outbox (purchases/collections/POS)
========================================================================
Ensure the offline outbox cannot create duplicates on retries.

Do:
- Confirm Go handlers/services use idempotency keys for:
  - POS checkout
  - purchases quick create
  - collections create
- Add regression tests:
  - two identical requests with same idempotency key return the original record (no duplicates).
- Ensure the DB unique indexes exist (via migrations from Prompt 07).

Acceptance:
- Tests prove idempotency behavior for all 3 critical flows.
- Flutter outbox still functions (manual QA steps documented).

========================================================================
PROMPT 08A — Offline-first robustness (beyond “queue and replay”)
========================================================================
Market expectation: offline must not corrupt stock/cash or silently duplicate transactions.

Do:
- Add a “Sync health” screen in Flutter:
  - queued/failed items list
  - per-item error, attempts, created_at
  - actions: retry, discard (with confirmation), export debug bundle
- Add conflict strategy:
  - Server-authoritative for inventory/cash totals.
  - For write endpoints: return stable identifiers so the client can reconcile local queued items.
- Add basic sync telemetry in backend logs (request_id + idempotency_key + entity_id created/returned).

Acceptance:
- Operators can understand and recover from failed sync without developer help.
- No write endpoint returns ambiguous success without IDs.

========================================================================
PROMPT 09 — Fix Drawer submenu routing bug (customer-facing UX bug)
========================================================================
Current issue: in the phone drawer (`DashboardSidebar`), tapping submenu items like “Cash Register” can route to a generic page instead of the real module screens.

Do:
- Make `flutter_app/lib/features/dashboard/presentation/widgets/dashboard_sidebar.dart` emit a structured navigation event (not just a label string), OR
- Handle submenu labels in `DashboardScreen._onSelectSecondaryByLabel` and route to the correct pages.

Acceptance:
- Every drawer submenu item routes to a real screen (no generic “label page”).
- Add a small widget test to prevent regression.

========================================================================
PROMPT 09A — Cash session lifecycle hardening (shift-safe operations)
========================================================================
Market expectation: cash sessions must be auditable, recoverable, and fraud-resistant.

Do:
- Backend:
  - Ensure cash register “open/close/tally” is modeled as a session with:
    - opening float, closing count, variance
    - cash drops/payouts (cash in/out) with reason codes
    - forced close by admin + reason (for lost devices)
    - immutable audit log entries for each event
- Flutter:
  - Implement a guided “Day End” flow:
    - cash count by denominations → variance → Z report export/share → close session
  - Permission gating: require manager override (PIN/password) for:
    - large discounts/voids/returns
    - cash drops/payouts

Acceptance:
- You can’t end up with “stuck open forever” sessions without an admin override path.
- Every cash movement is attributable to a user + device + request_id.

========================================================================
PROMPT 09B — Training mode (non-posting) + clear operator UX
========================================================================
Market expectation: stores need a safe way to train new staff without polluting real totals, inventory, or reports.

Do:
- Backend:
  - Support a “training mode” that prevents posting to real ledgers/inventory/cash totals.
  - Ensure training transactions are either:
    - stored separately (recommended), OR
    - stored with a flag and excluded from all operational reports by default.
  - Audit log should still record who entered training mode and what was done.
- Flutter:
  - Add an explicit “Training mode” toggle (admin/manager only), scoped to the current register/session.
  - Always show a persistent banner/watermark while in training mode.
  - Prevent training transactions from being queued into the offline outbox for real posting.

Acceptance:
- Training sales never change stock/cash totals, even after reconnect/sync.
- The operator can’t “accidentally” stay in training mode (clear banner + exit flow).

========================================================================
PROMPT 10 — Finish Quick Action Button flows (Purchase / Collection / Expense)
========================================================================
Current issue: only “Sale” quick action is wired.

Do:
- Wire quick actions:
  - Purchase → quick purchase / GRN entry
  - Collection → quick collection entry
  - Expense → quick expense entry
- Ensure each flow:
  - validates location selection
  - has loading/error/success states
  - supports offline outbox where applicable

Acceptance:
- All quick actions are usable end-to-end.
- Offline behavior queues writes and shows status in the header.

========================================================================
PROMPT 11 — Notifications: replace demo screen with real feature
========================================================================
Current issue: `NotificationsPage` is hard-coded demo data.

Do:
- Decide the minimal notification scope for v1:
  - low stock alerts
  - sync failures
  - approvals (workflow)
- If backend lacks endpoints, add:
  - DB table(s)
  - endpoints to list/mark-read
  - OpenAPI docs + tests
- Update Flutter UI:
  - real list + unread badge + mark read

Acceptance:
- No demo data remains reachable in production builds.
- Parity checks remain clean.

========================================================================
PROMPT 12 — Settings module completeness (tax/invoice/printer/payment/device/session)
========================================================================
Implement real settings screens and wire to backend endpoints already in OpenAPI.

Do:
- Build Settings subpages for:
  - Tax settings
  - Invoice settings/templates
  - Printer settings
  - Payment methods + currencies
  - Device control + session limits
- Use consistent empty/loading/error states and reuse existing theme components.

Acceptance:
- Settings tiles are not placeholders; each opens a working screen.
- CRUD operations succeed and errors are user-friendly.

========================================================================
PROMPT 13 — Admin: Users/Roles/Permissions UI (core ERP requirement)
========================================================================
Backend exposes users/roles/permissions; Flutter must provide admin UX.

Do:
- Implement admin screens to manage:
  - Users
  - Roles
  - Role permissions
- Ensure UI respects RBAC (hide/disable unauthorized actions).

Acceptance:
- Admin can manage users/roles end-to-end from Flutter.
- OpenAPI parity remains clean; add minimal Flutter tests.

========================================================================
PROMPT 14 — Device sessions UI (security + operations)
========================================================================
Wire device session endpoints to Flutter for admins/users.

Do:
- Add a UI to list active device sessions and revoke a session.
- Ensure session limits UI is connected (if supported by backend).

Acceptance:
- A user/admin can see sessions and revoke them.

========================================================================
PROMPT 15 — Workflow requests UI (approvals)
========================================================================
Backend has workflow request endpoints; Flutter needs an approvals inbox.

Do:
- Implement a workflow requests screen:
  - list requests
  - view details
  - approve/reject with remarks

Acceptance:
- Approvals work end-to-end and update list state.

========================================================================
PROMPT 16 — Import/Export flows (Inventory/Customers/Suppliers)
========================================================================
Ensure SMB onboarding works (bulk import/export).

Do:
- Add UI for:
  - customer import/export
  - supplier import/export
  - inventory import/export
- If backend export returns files, ensure Flutter handles downloads + share.

Acceptance:
- Import/export is discoverable and works on at least one platform (Windows/Android).

========================================================================
PROMPT 17 — Reports: completeness + export reliability
========================================================================
Confirm required reports exist and exports work consistently.

Do:
- Cross-check required reports from requirements docs vs implemented endpoints.
- Ensure every report screen:
  - supports required filters (date range/location/etc.)
  - supports export PDF/Excel
  - shows clear errors when backend returns JSON errors during “bytes” downloads

Acceptance:
- All required report categories are implemented and export works.
- Parity tooling remains clean (no dynamic `/reports/{}` paths).

========================================================================
PROMPT 18 — POS completeness audit (SMB ERP essentials)
========================================================================
Audit POS flows against `ebs_lite_win/Requirements.txt`.

Do:
- Verify and implement missing essentials:
  - split payments
  - multi-currency tender/exchange rates (if in scope)
  - credit limit checks
  - loyalty earn/redeem + receipts
  - returns by reference
  - reprint/share receipt/invoice

Acceptance:
- A written POS checklist with “implemented vs missing” and fixes merged for missing items.

========================================================================
PROMPT 18A — Returns/void/discount controls (shrinkage prevention)
========================================================================
Market expectation: SMB operators need guardrails for high-risk actions.

Do:
- Backend:
  - Enforce role-based limits:
    - max line discount / max bill discount per role
    - return/void permissions with mandatory reason + audit log
  - Require manager override token/PIN for actions beyond staff limits.
- Flutter:
  - UI prompts for reason + manager override when required.
  - Display “why action is blocked” clearly to staff.

Acceptance:
- A cashier cannot bypass limits by manipulating the UI payload.
- Audit logs capture who approved overrides.

========================================================================
PROMPT 18B — Hardware integrations + receipt/print reliability (POS reality)
========================================================================
POS correctness is not enough: real stores require reliable printing/scanning/drawer behavior on the supported platforms.

Do:
- Flutter:
  - Validate receipt printing end-to-end:
    - ESC/POS 80mm/58mm (thermal) + cash drawer kick
    - A4/A5 invoice/receipt export/share
    - reprint flows from sale history
  - Validate barcode scanner behavior (keyboard wedge + camera scan if used) across Windows/Android.
  - Ensure printer settings screens are real (no placeholders) and persist per device/session where appropriate.
- Backend:
  - Ensure receipt/invoice data endpoints provide stable, complete payloads for printing (no missing totals/taxes).
  - Ensure print/export endpoints handle “bytes vs JSON error” consistently (see Prompt `17`).

Acceptance:
- A smoke checklist exists and is executed on at least Windows + Android:
  - print, reprint, share, open drawer, scan, offline banner behavior.
- No placeholder printing/settings screens are reachable in production builds.

========================================================================
PROMPT 19 — Backend performance + correctness audit (hot paths)
========================================================================
Focus on likely hot endpoints (POS, products, stock, sales history).

Do:
- Identify and remove obvious N+1 query patterns.
- Avoid expensive per-request DB writes in hot middleware paths.
- Add/adjust tests to keep behavior stable.

Acceptance:
- Go tests pass and the performance fixes are measurable (basic before/after notes).

========================================================================
PROMPT 20 — Final quality gate + release checklist
========================================================================
Do a final end-to-end cleanup pass.

Do:
- Re-run all DoD checks (Go/Flutter/Parity/Formatting).
- Ensure OpenAPI is accurate and complete.
- Ensure no placeholder/demo screens are reachable in production builds.
- Produce a final release checklist (signing, flavors, env vars, backups, migration steps).

Acceptance:
- All checks pass with zero drift.
- A final checklist exists and is actionable for a release candidate build.
